name: 🤖 AI Auto Update Log

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-update:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[skip ci]') && !contains(github.event.head_commit.message, 'Auto AI Update Log')"

    steps:
      - name: 🚀 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📝 Get commit and optimized file changes
        run: |
          echo "=== TRIGGERING COMMIT ==="
          git log -1 --pretty=%B > commit_message.txt
          echo "=== CHANGED FILES ==="
          git diff --name-only HEAD~1 HEAD > changed_files.txt 2>/dev/null || echo "Initial commit" > changed_files.txt
          
          # Get optimized diffs (only key changes, not full files)
          echo "=== OPTIMIZED FILE CHANGES ===" > file_changes.txt
          while IFS= read -r file; do
            if [[ -n "$file" && "$file" != "Initial commit" ]]; then
              echo "--- $file ---" >> file_changes.txt
              # Get only the actual changed lines, limit to 20 lines per file
              git diff --unified=0 HEAD~1 HEAD -- "$file" | grep -E "^(\+|\-)" | head -20 >> file_changes.txt 2>/dev/null || echo "No significant changes detected" >> file_changes.txt
              echo "" >> file_changes.txt
            fi
          done < changed_files.txt
          
          cat commit_message.txt
          cat changed_files.txt
          echo "=== OPTIMIZED CHANGES PREVIEW ==="
          head -30 file_changes.txt

      - name: 🤖 Generate AI update block (Gemini)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 python3-pip
          pip3 install requests

          python3 << 'EOF'
          import requests, os, datetime, random

          # Read commit, file changes, and optimized diffs
          commit_message = open("commit_message.txt").read().strip()
          changed_files = open("changed_files.txt").read().strip()
          file_changes = open("file_changes.txt").read().strip()
          today = datetime.date.today().strftime("%B %d, %Y")
          
          # Generate version number only (AI will create the title)
          version_number = f"1.{random.randint(1,20)}.{random.randint(0,9)}"

          api_key = os.environ["GEMINI_API_KEY"]
          url = f"https://generativelanguage.googleapis.com/v1/models/gemini-2.5-flash:generateContent?key={api_key}"
          
          print(f"🎯 Commit: {commit_message}")
          print(f"📁 Changed files: {changed_files}")
          print(f"📄 Optimized changes preview: {file_changes[:300]}...")

          prompt = f"""
          Create an update card for an unblocked games website by analyzing the code changes.

          COMMIT MESSAGE: "{commit_message}"
          CHANGED FILES: {changed_files}

          KEY CODE CHANGES:
          {file_changes}

          Analyze these changes and create:
          1. A RELEVANT TITLE that summarizes the changes
          2. ONE accurate description that explains what was changed and how it affects users

          Focus on:
          - Game additions/removals (look for game names)
          - UI/design changes  
          - Functionality improvements
          - Bug fixes

          Return ONLY this HTML structure:

          <div class="update-item" style="animation-delay: 0.1s;">
            <div class="update-header">
              <div class="version-info">
                <span class="version-badge">v{version_number}</span>
                <h3 class="version-title">[AI-GENERATED TITLE]</h3>
              </div>
              <span class="version-date">{today}</span>
            </div>
            <div class="update-content">
              <ul class="update-list">
                <li>[ACCURATE DESCRIPTION WITH EMOJI]</li>
              </ul>
            </div>
          </div>
          """

          try:
              payload = {
                  "contents": [
                      {"parts": [{"text": prompt}]}
                  ],
                  "generationConfig": {
                      "temperature": 0.7,
                      "topK": 40,
                      "topP": 0.95,
                      "maxOutputTokens": 800,
                  }
              }

              print("🚀 Sending request to Gemini API...")
              response = requests.post(url, json=payload, timeout=30)
              print(f"📡 Response status: {response.status_code}")
              
              if response.status_code != 200:
                  print(f"❌ API Error {response.status_code}: {response.text}")
                  raise Exception(f"API returned {response.status_code}")
              
              result = response.json()
              
              # Extract text from response
              ai_text = ""
              if "candidates" in result and len(result["candidates"]) > 0:
                  candidate = result["candidates"][0]
                  if "content" in candidate and "parts" in candidate["content"] and len(candidate["content"]["parts"]) > 0:
                      ai_text = candidate["content"]["parts"][0].get("text", "")
              
              if not ai_text:
                  print(f"❌ No text in response: {result}")
                  raise Exception("No text content in API response")
              
              # Clean the response
              cleaned_text = ai_text.strip()
              cleaned_text = cleaned_text.replace('```html', '').replace('```', '').strip()
              
              # Save the AI-generated content
              with open("new_update.html", "w") as f:
                  f.write(cleaned_text)
                  
              print("✅ AI-generated content saved!")
              print("📝 Generated content:")
              print(cleaned_text)
              
          except Exception as e:
              print(f"❌ AI generation failed: {e}")
              raise Exception("AI generation failed - no fallback template used")

          EOF

      - name: 📋 Safe HTML insertion using Python
        run: |
          python3 << 'EOF'
          import re
          
          # Read the generated update
          with open("new_update.html", "r") as f:
              new_update = f.read().strip()
          
          # Read the current ul.html
          with open("ul.html", "r") as f:
              content = f.read()
          
          # Remove existing content between the markers
          pattern = r'(<!-- UPDATE_LOG_START -->\s*).*?(\s*<!-- UPDATE_LOG_END -->)'
          replacement = r'\1' + new_update + r'\2'
          
          new_content = re.sub(pattern, replacement, content, flags=re.DOTALL)
          
          # Write the updated content back
          with open("ul.html", "w") as f:
              f.write(new_content)
          
          print("✅ Update successfully inserted into ul.html")
          EOF

      - name: 🔍 Verify the update was inserted
        run: |
          echo "=== VERIFYING INSERTION ==="
          if grep -q "update-item" ul.html; then
            echo "✅ Update card found in ul.html"
            echo "=== FULL INSERTED CONTENT ==="
            sed -n '/<!-- UPDATE_LOG_START -->/,/<!-- UPDATE_LOG_END -->/p' ul.html
          else
            echo "❌ Error: Update card not found in HTML"
            exit 1
          fi

      - name: 📤 Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add ul.html
          git diff --staged --quiet || git commit -m "🤖 AI Auto Update: $(date +'%Y-%m-%d %H:%M')"
          git push

      - name: 🧹 Cleanup
        run: rm -f new_update.html commit_message.txt changed_files.txt file_changes.txt
