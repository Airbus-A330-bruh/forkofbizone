name: 🤖 AI Auto Update Log

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-update:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[skip ci]') && !contains(github.event.head_commit.message, 'Auto AI Update Log')"

    steps:
      - name: 🚀 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📝 Get recent commits context
        run: |
          echo "=== TRIGGERING COMMIT ==="
          git log -1 --pretty=%B > commit_message.txt
          cat commit_message.txt

      - name: 🤖 Generate AI update block (Gemini)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 python3-pip
          pip3 install requests

          python3 << 'EOF'
          import requests, os, datetime, random, json

          # Read the actual commit message that triggered this
          commit_message = open("commit_message.txt").read().strip()
          today = datetime.date.today().strftime("%B %d, %Y")
          
          version_names = ["Quantum", "Phoenix", "Nebula", "Velocity", "Horizon", "Infinity", "Cosmic", "Synergy"]
          version_adjectives = ["Update", "Release", "Launch", "Drop", "Upgrade", "Evolution"]
          
          version_name = f"{random.choice(version_names)} {random.choice(version_adjectives)}"
          version_number = f"1.{random.randint(1,20)}.{random.randint(0,9)}"

          api_key = os.environ["GEMINI_API_KEY"]
          
          # Use the CORRECT Gemini model that actually exists
          url = f"https://generativelanguage.googleapis.com/v1/models/gemini-1.5-pro:generateContent?key={api_key}"
          
          print(f"🎯 Using commit message: {commit_message}")

          prompt = f"""
          Create an engaging HTML update card for an unblocked games website based on this git commit: "{commit_message}"
          
          Return ONLY the HTML code with no explanations. Use this exact structure:

          <div class="update-item" style="animation-delay: 0.1s;">
            <div class="update-header">
              <div class="version-info">
                <span class="version-badge">v{version_number}</span>
                <h3 class="version-title">{version_name}</h3>
              </div>
              <span class="version-date">{today}</span>
            </div>
            <div class="update-content">
              <ul class="update-list">
                <li>✨ Create 3-4 specific bullet points based on the commit</li>
                <li>🎮 Use gaming emojis and exciting language</li>
                <li>🚀 Make it relevant to game website updates</li>
                <li>🎯 Be specific about what changed</li>
              </ul>
            </div>
          </div>
          """

          try:
              payload = {
                  "contents": [
                      {"parts": [{"text": prompt}]}
                  ],
                  "generationConfig": {
                      "temperature": 0.9,
                      "topK": 40,
                      "topP": 0.95,
                      "maxOutputTokens": 500,
                  }
              }

              print("🚀 Sending request to Gemini API...")
              response = requests.post(url, json=payload, timeout=30)
              print(f"📡 Response status: {response.status_code}")
              
              if response.status_code != 200:
                  print(f"❌ API Error {response.status_code}: {response.text}")
                  raise Exception(f"API returned {response.status_code}")
              
              result = response.json()
              ai_text = result["candidates"][0]["content"]["parts"][0]["text"]
              
              # Clean the response
              cleaned_text = ai_text.strip()
              cleaned_text = cleaned_text.replace('```html', '').replace('```', '').strip()
              
              # Save the AI-generated content
              with open("new_update.html", "w") as f:
                  f.write(cleaned_text)
                  
              print("✅ AI-generated content saved!")
              
          except Exception as e:
              print(f"❌ AI generation failed: {e}")
              # Create a SIMPLE fallback that's still based on the commit
              print("🔄 Creating commit-specific fallback...")
              
              # Extract keywords from commit to make it specific
              commit_lower = commit_message.lower()
              if any(word in commit_lower for word in ['game', 'add', 'new']):
                  items = [
                      f"🎮 Added new games based on: {commit_message}",
                      "🚀 Improved gaming experience and performance",
                      "✨ Enhanced website features and user interface",
                      "🎯 Better gameplay and bug fixes"
                  ]
              elif any(word in commit_lower for word in ['fix', 'bug', 'error']):
                  items = [
                      f"🐛 Fixed issues: {commit_message}",
                      "🚀 Improved stability and performance",
                      "✨ Enhanced user experience",
                      "🎮 Better gameplay functionality"
                  ]
              elif any(word in commit_lower for word in ['update', 'change', 'modify']):
                  items = [
                      f"🔄 Updated: {commit_message}",
                      "🚀 Enhanced website performance",
                      "✨ Improved user interface and features",
                      "🎮 Better gaming experience"
                  ]
              else:
                  items = [
                      f"🎯 Changes: {commit_message}",
                      "🚀 Performance improvements and updates",
                      "✨ Enhanced gaming website features",
                      "🎮 Better user experience and gameplay"
                  ]
              
              fallback_html = f'''
              <div class="update-item" style="animation-delay: 0.1s;">
                <div class="update-header">
                  <div class="version-info">
                    <span class="version-badge">v{version_number}</span>
                    <h3 class="version-title">{version_name}</h3>
                  </div>
                  <span class="version-date">{today}</span>
                </div>
                <div class="update-content">
                  <ul class="update-list">
                    {''.join([f'<li>{item}</li>' for item in items])}
                  </ul>
                </div>
              </div>
              '''
              
              with open("new_update.html", "w") as f:
                  f.write(fallback_html.strip())
              print("📁 Commit-specific fallback saved")

          EOF

      - name: 📋 Safe HTML insertion using Python
        run: |
          python3 << 'EOF'
          import re
          
          # Read the generated update
          with open("new_update.html", "r") as f:
              new_update = f.read().strip()
          
          # Read the current ul.html
          with open("ul.html", "r") as f:
              content = f.read()
          
          # Remove existing content between the markers
          pattern = r'(<!-- UPDATE_LOG_START -->\s*).*?(\s*<!-- UPDATE_LOG_END -->)'
          replacement = r'\1' + new_update + r'\2'
          
          new_content = re.sub(pattern, replacement, content, flags=re.DOTALL)
          
          # If markers don't exist, create them
          if new_content == content:
              marker = "<!-- UPDATE_LOG_START -->\n            " + new_update + "\n            <!-- UPDATE_LOG_END -->"
              if "<!-- UPDATE_LOG_START -->" not in content:
                  # Insert after the version-section div
                  new_content = content.replace(
                      '<div class="version-section">', 
                      '<div class="version-section">\n\n            <!-- UPDATE_LOG_START -->\n            ' + new_update + '\n            <!-- UPDATE_LOG_END -->'
                  )
          
          # Write the updated content back
          with open("ul.html", "w") as f:
              f.write(new_content)
          
          print("✅ Update successfully inserted into ul.html")
          EOF

      - name: 🔍 Verify the update was inserted
        run: |
          echo "=== VERIFYING INSERTION ==="
          if grep -q "update-item" ul.html; then
            echo "✅ Update card found in ul.html"
            echo "=== INSERTED CONTENT PREVIEW ==="
            grep -A 10 "<!-- UPDATE_LOG_START -->" ul.html | head -15
          else
            echo "❌ Error: Update card not found in HTML"
            exit 1
          fi

      - name: 📤 Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add ul.html
          git diff --staged --quiet || git commit -m "🤖 AI Auto Update: $(date +'%Y-%m-%d %H:%M')"
          git push

      - name: 🧹 Cleanup
        run: rm -f new_update.html commit_message.txt
